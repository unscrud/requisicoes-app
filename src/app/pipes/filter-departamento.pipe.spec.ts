import { FilterDepartamentoPipe } from './filter-departamento.pipe';

describe('FilterDepartamentoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDepartamentoPipe();
    expect(pipe).toBeTruthy();
  });
});
